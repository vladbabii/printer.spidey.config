[gcode_macro SPEED_PERCENTAGE]
variable_speed: 100
variable_accel: 100
gcode:
  RESPOND PREFIX="info" MSG="SPEED_PERCENTAGE: {printer['gcode_macro SPEED_PERCENTAGE'].speed} / {printer['gcode_macro SPEED_PERCENTAGE'].accel}"